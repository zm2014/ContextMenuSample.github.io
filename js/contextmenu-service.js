app.service('ContextmenuService', function () {

    /*Alert message, as set in the controller*/
    this.evenNumberExplorer = function (message) {
        alert("My message : " + message);
    };

    this.oddNumberExplorer = function (message) {
        alert("My message : " + message);
    };
});

